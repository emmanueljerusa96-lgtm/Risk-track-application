{
  "name": "risk-track-functions",
  "private": true,
  "version": "1.0.0",
  "description": "Trusted report review notifications for Risk Track",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "node --test test/messages.test.js",
    "lint": "node --check index.js && node --check messages.js",
    "test:rules": "node --test test/rules.test.js"
  },
  "dependencies": {
    "firebase-admin": "^14.5.0",
    "firebase-functions": "^7.4.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0"
  }
}
